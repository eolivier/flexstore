spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flexstore
    username: flex
    password: flex
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
